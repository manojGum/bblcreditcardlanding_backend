'use strict';

/**
 * card-leads-heading router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::card-leads-heading.card-leads-heading');
