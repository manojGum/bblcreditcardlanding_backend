'use strict';

/**
 * card-leads-heading service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::card-leads-heading.card-leads-heading');
